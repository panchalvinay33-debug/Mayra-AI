<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <uses-feature android:name="android.hardware.telephony" android:required="false" />
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:name=".MayraApplication"
        android:allowBackup="false"
        android:usesCleartextTraffic="false"
        android:label="Mayra AI"
        android:theme="@style/Theme.MayraAI">

        <!-- The app intentionally exposes exactly one launcher entry. -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Feature screens are reachable only from the Mayra home screen. -->
        <activity
            android:name=".document.MayraDocumentActivity"
            android:exported="false"
            android:label="Mayra Library" />
        <activity
            android:name=".memory.MayraMemoryCenterActivity"
            android:exported="false"
            android:label="Mayra Memory Center" />
        <activity
            android:name=".core.MayraProviderSettingsActivity"
            android:exported="false"
            android:label="Mayra AI Provider" />
        <activity
            android:name=".document.MayraDocumentHealthActivity"
            android:exported="false"
            android:label="Mayra Library Health" />
        <activity
            android:name=".core.MayraActivityHistoryActivity"
            android:exported="false"
            android:label="Mayra Activity History" />

        <service
            android:name=".background.MayraNotificationListener"
            android:exported="false"
            android:label="Mayra notification intelligence"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
            <meta-data
                android:name="android.service.notification.default_filter_types"
                android:value="conversations|alerting" />
            <meta-data
                android:name="android.service.notification.disabled_filter_types"
                android:value="ongoing|silent" />
        </service>

        <receiver
            android:name=".reminder.MayraReminderActionReceiver"
            android:enabled="true"
            android:exported="false" />

        <receiver
            android:name=".background.MayraBootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
