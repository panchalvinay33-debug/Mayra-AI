<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      The CI/distributed debug APK is intentionally permission-minimal for local validation.
      Production capability declarations remain in the main manifest, while debug builds use
      system pickers and app-private storage without network, contacts, microphone,
      notifications or boot access.
    -->
    <uses-permission
        android:name="android.permission.INTERNET"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.RECORD_AUDIO"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.READ_CONTACTS"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.POST_NOTIFICATIONS"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.RECEIVE_BOOT_COMPLETED"
        tools:node="remove" />

    <application
        android:label="Mayra Library Test"
        tools:replace="android:label">

        <service
            android:name=".background.MayraNotificationListener"
            tools:node="remove" />

        <receiver
            android:name=".background.MayraBootReceiver"
            tools:node="remove" />
    </application>
</manifest>
